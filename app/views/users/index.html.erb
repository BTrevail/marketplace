<h1>All Users</h1>

<% @users.each do |user| %>

	<ul>
		<li><%= link_to user.username, user_path(user) %></li>
		<li><%= pluralize(user.products.count, "product") if user.products %></li>
	</ul>

<% end %>
