<% if obj.errors.any? %>
  <h2>User was not created due to the following errors:</h2>
  <ul>
    <% obj.errors.full_messages.each do |msg| %>

      <li><%= msg %></li>
    <% end %>
  </ul>>
<% end %>